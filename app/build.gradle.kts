// app/build.gradle.kts 파일은 이렇게 시작해야 합니다.
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    // ... 기타 플러그인들
}

android {
    namespace = "com.example.helper"
    compileSdk = 34
    // ... 나머지 설정
}
